# ==============================================
# ACA Docker Compose - Environment Variables
# ==============================================
# Copy this file to .env and fill in actual values:
#   cp .env.docker.example .env
#
# NEVER commit .env with real secrets to version control.
# ==============================================

# ==============================================
# Docker Compose Port Mapping
# ==============================================
# Host ports exposed by docker compose.
# Change these if 3000/8080 conflict with other services.
API_PORT=3000
WEB_PORT=8080

# ==============================================
# Authentication (REQUIRED)
# ==============================================
# JWT signing secret. Must be at least 16 characters.
# Generate one with: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_minimum_16_chars

# Admin credentials for the dashboard login.
ACA_ADMIN_EMAIL=admin@aca.local
ACA_ADMIN_PASSWORD=CHANGE_ME_strong_password

# ==============================================
# LLM API Keys (at least one REQUIRED)
# ==============================================
# Anthropic Claude
ANTHROPIC_API_KEY=sk-ant-xxx

# OpenAI (optional)
# OPENAI_API_KEY=sk-xxx

# Google Gemini (optional)
# GEMINI_API_KEY=xxx

# Default LLM provider: claude | openai | gemini
LLM_PROVIDER=claude

# LLM tuning
LLM_MAX_TOKENS=4096
LLM_TEMPERATURE=0.3
# LLM_MODEL=claude-sonnet-4-5-20250929

# ==============================================
# GitHub Configuration
# ==============================================
GITHUB_TOKEN=ghp_xxx
# GITHUB_OWNER=your-username
# GITHUB_REPO=your-repo

# ==============================================
# Runner Configuration
# ==============================================
# Working directory inside the container
WORK_DIR=/data/workspace

MAX_CONCURRENT_TASKS=3
RETRY_ATTEMPTS=3
TASK_TIMEOUT=600000
MAX_CONCURRENT_FEATURES=3
MAX_TURNS_PER_FEATURE=100
AGENT_TIMEOUT_MINUTES=480

# ==============================================
# Feature Flags
# ==============================================
ENABLE_VALIDATION=true
ENABLE_LEARNING=true
ENABLE_CONTEXT_MANAGEMENT=true
USE_REAL_QUALITY_TOOLS=false

# ==============================================
# Multi-Model Routing
# ==============================================
ROUTING_ENABLED=false
ROUTING_BUDGET_LIMIT=50.00

# ==============================================
# Logging
# ==============================================
LOG_LEVEL=info
LOG_TO_FILE=true
LOG_DIR=/data/logs

# ==============================================
# CORS (managed by docker-compose, override if needed)
# ==============================================
# CORS_ORIGINS is set in docker-compose.yml by default.
# Uncomment below to override with your production domain:
# CORS_ORIGINS=https://dashboard.example.com
