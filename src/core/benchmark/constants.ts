/**
 * Benchmark Constants
 *
 * Named constants extracted from benchmark runner and performance profiler.
 *
 * @module core/benchmark
 */

/** Default cost estimate per 1,000 tokens (USD) */
export const DEFAULT_COST_PER_1K_TOKENS = 0.003;

/** Multiplier for computing the 95th-percentile index in a sorted array */
export const P95_PERCENTILE = 0.95;
